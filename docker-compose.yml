services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: qrcheck
      POSTGRES_PASSWORD: qrcheck
      POSTGRES_DB: qrcheck
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
